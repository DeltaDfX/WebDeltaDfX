import { JSGroupStakeholder } from './jsgroup-stakeholder';

describe('JSGroupStakeholder', () => {
  it('should create an instance', () => {
    expect(new JSGroupStakeholder()).toBeTruthy();
  });
});
